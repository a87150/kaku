# Kaku supervisor 配置示例（生产部署参考）
# 请按实际环境调整路径与用户
[program:kaku]
command = /django/deploy/gunicorn_start.sh    ; 启动脚本
user = kk                                     ; 运行用户
stdout_logfile = /django/logs/gunicorn_supervisor.log
redirect_stderr = true
